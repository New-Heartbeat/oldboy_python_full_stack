重点1：
    名词查找：当前所在位置向外查找
    局部名称空间=>全局名称空间=>内置名称空间

重点2：
    名称空间只有优先级之分，本身并无嵌套关系，画图只是为了理解

重点3：
    名称空间得嵌套关系决定了名字的查找关系
    而名称空间的嵌套关系是以函数定义阶段为主的
    即函数的嵌套关系与名字的查找顺序是在定义阶段就已经确定好的

今日内容：
    1、函数对象（可以把函数当成变量去用）

    2、函数嵌套

    3、闭包函数


